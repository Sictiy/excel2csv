package {{.Package}}.bean;

import lombok.Getter;
import lombok.ToString;
{{range .Imports -}}
import {{.}};
{{end}}
import com.sictiy.common.entry.annotation.CommomAnnotation;

/**
* {{ .Comment }}
*
* generated by tool
* @since: {{ getDate }}
*/
@Getter
@ToString
@CommomAnnotation(str = "{{ .Name }}")
public class {{.JavaName}}Bean
{{ "{" }}{{range .Columns}}
    /**
    * {{ .Comment }}
    */
    {{ .CsvBind}}
    private {{ .JavaType }} {{ .Name }};
{{end}}{{ "}" }}